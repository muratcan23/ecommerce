import RegisterClient from "../components/auth/RegisterClient";

const Register = () => {
  return (
    <div>
      <RegisterClient currentUser={undefined} />
    </div>
  );
};

export default Register;
